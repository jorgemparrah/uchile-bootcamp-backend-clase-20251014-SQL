Table AUTOPISTA {
  id VARCHAR(20) [primary key]
  nombre VARCHAR(50) [not null]
  ciudad_origen VARCHAR(50)
  ciudad_destino VARCHAR(50)
}

Table PEAJE {
  id integer [primary key]
  id_autopista VARCHAR(20) [ref: > AUTOPISTA.id, not null]
}

Table CONDUCTOR {
  rut VARCHAR(12) [primary key]
  licencia VARCHAR(100) [ref: > LICENCIA.id ]
}

Table LICENCIA {
  id VARCHAR(100) [primary key]
  nombre VARCHAR(100) [not null]
  apellido VARCHAR(100) [not null]
  fecha_vencimiento DATE [not null]
}

Table VEHICULO {
  patente VARCHAR(6)
  modelo VARCHAR(6)
  fabricacion YEAR
  rut_conductor VARCHAR(12) [ref: > CONDUCTOR.rut ]
}

Table PASE {
  id_transaccion VARCHAR(100) [primary key]
  patente_vehiculo VARCHAR(6) [not null, ref: > VEHICULO.patente ]
  rut_conductor VARCHAR(12) [not null, ref: > CONDUCTOR.rut ]
  id_peaje INTGER [not null, ref: > PEAJE.id ]
  fecha_hora DATETIME [not null]
}